import StyledSearch from "../../styles/NavbarStyles/Search.styled";

const Search: React.FC = (): JSX.Element => {
  return (
    <StyledSearch>
      <p>Search</p>
    </StyledSearch>
  );
};

export default Search;
