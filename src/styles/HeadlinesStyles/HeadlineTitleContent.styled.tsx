import styled from "styled-components";

const StyledHeadlineTitleContent = styled.div`
  border-bottom: 1px solid red;

  &:last-of-type {
    border: none;
  }
`;

export default StyledHeadlineTitleContent;
